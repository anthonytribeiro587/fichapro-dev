import { AppShell } from '@/components/AppShell';
import { NewOrderWorkspace } from '@/components/orders/NewOrderWorkspace';

export default function NovoPedidoPage() {
  return (
    <AppShell>
      <NewOrderWorkspace />
    </AppShell>
  );
}
